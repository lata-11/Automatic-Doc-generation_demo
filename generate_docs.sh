#!/bin/bash
#can test this by running command ./generate_docs.sh


echo "Generating RM GSMA docs..."
# Replace the above command with the actual command to generate the docs

echo "RM GSMA docs generated successfully."
